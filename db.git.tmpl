;
; BIND data file for domain: git
;
$TTL    604800
@       IN      SOA     git. root.git. (
			{BIND_DB_GIT_SERIAL}	; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
;
